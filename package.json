{
  "scripts": {
    "typecheck": "tsc -p . --noEmit",
    "build": "npm run typecheck && node ./scripts/build-gas.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "@types/google-apps-script": "^2.0.8"
  }
}
